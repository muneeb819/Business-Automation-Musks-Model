'use client';

import Placeholder from '../../../components/Placeholder';

export default function WorkflowsPage() {
  return (
    <Placeholder
      title="Workflows"
      description="Durable multi-step agent workflows (e.g. the 30-day outreach lifecycle)."
    />
  );
}
